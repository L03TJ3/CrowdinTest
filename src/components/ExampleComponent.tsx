import { Button, YStack } from "tamagui";
// import { createPortal } from "react-dom";

export const ExampleComponent = () => {
  return (
    <YStack>
      <Button>1</Button>
      <Button>2</Button>
      <Button>3</Button>
      <Button>+</Button>
    </YStack>
  );
};
